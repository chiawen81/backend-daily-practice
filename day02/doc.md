# Backend Daily 02 — C# 變數與基本型別

請幫我建立今天的 Daily 練習環境，但**不要替我完成 Coding Task**。

## 今日主題

C# 基本變數宣告：

~~~csharp
型別 變數名稱 = 值;
~~~

今天先使用明確型別，不使用 `var`。

目前會使用：

- `int`
- `string`
- `bool`

## Coding Task

請在 Console Project 的 `Program.cs` 中自行完成以下需求。

商品資料：

- 商品名稱：Mechanical Keyboard
- 價格：2500
- 是否有庫存：true
- 庫存數量：8

要求：

1. 自己判斷四個變數應使用的 C# 型別。
2. 變數名稱使用 camelCase。
3. 不使用 `var`。
4. 使用 `Console.WriteLine()` 輸出四個值。
5. 執行程式確認結果。

## 今日練習目標

需求中的資料
→ 判斷型別
→ 用 C# 宣告變數
→ 使用變數

## Codex 注意事項

- 不要提供 Coding Task 的答案。
- 不要替我撰寫 `Program.cs` implementation。
- 不要新增額外學習內容。
- 只協助建立／準備今天的練習環境。